# Deprecato: usare invece
#   - deploy/nginx-gesper.example.conf (solo gesper1.plazapretoria.it)
#   - deploy/nginx_www_plazapretoria.example.conf (www + /gesper/ su porta 8003)
#
# Il prefisso /gesper/ richiede Gunicorn con DJANGO_SETTINGS_MODULE=settings_production_www
# (vedi deploy/gesper-www.service.example).
